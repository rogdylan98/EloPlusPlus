
const QuestionDetails = (question) => {
    console.log(question);
    // const questionSpread = [...question];
    // console.log(questionSpread);
    return (
        <main>
            <div>
                {/* {questionSpread.map(field =>
                <div>
                    <h1>{field.title}</h1>
                    <h2>{field.body}</h2>
                </div>)} */}
                <h1>Test</h1>
            </div>
        </main>
    )
}

export default QuestionDetails
